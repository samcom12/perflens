# Mini App

This folder contains tiny source files in the languages perflens targets first:

- `stencil.c`
- `particles.cpp`
- `vector_loop.py`
- `saxpy.f90`

They are intentionally simple and include a few performance smells so the scanner, profile parser, optimizer, validator, and dashboard can be exercised without a full HPC build.

